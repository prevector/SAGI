{
  "name": "@sagi/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build -w @sagi/shared && tsc --noEmit -p tsconfig.json && vite build",
    "dev": "vite"
  },
  "dependencies": {
    "@sagi/shared": "0.1.0"
  },
  "devDependencies": {
    "vite": "^7.1.3"
  }
}
