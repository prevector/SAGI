{
  "name": "@sagi/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build -w @sagi/shared && npm run build -w @sagi/evolution && npm run build -w @sagi/ledger && tsc -p tsconfig.json",
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@faker-js/faker": "^10.5.0",
    "@sagi/evolution": "0.1.0",
    "@sagi/ledger": "^0.1.0",
    "@sagi/shared": "0.1.0",
    "better-sqlite3": "^12.11.1",
    "cors": "^2.8.5",
    "drizzle-orm": "^0.45.2",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "tsx": "^4.20.5"
  }
}
